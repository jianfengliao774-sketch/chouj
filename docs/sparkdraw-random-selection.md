# 网页随机选号

2026-09-07：按用户要求保留现有五个 V5 合约，不部署或切换新合约。自动模式改为「随机选号」，自由选号保持原规则。

网页在授权完成后，按同一区块读取当前期号、钱包份额及全部 556 个号码存储字。每个存储字含 18 个 14 位持有人编号，0 表示未售；末尾超出 10,000 的槽位不参与。使用浏览器 crypto.getRandomValues、拒绝取模偏差与部分 Fisher–Yates 洗牌，从未售号码中等概率、不重复取样。数量不超过请求份数、钱包本期剩余额度及实际未售数量。

取样后按合约要求升序编码，通过已有 buySelected 购买。排序只影响编码，不改变抽样集合。不使用旧 buy 的连续分配路径，也不因 RPC 失败回退成连续号段。钱包、页面选择或期号变化会停止本次提交。领取、退款、开奖随机数与后台自动执行均保持现有合约规则。

网页读取与链上成交之间仍可能有其他交易。冲突号码由现有合约向后补号并在末尾回绕；最终以 TicketsAllocated 与 PurchaseResult 为准。接近售罄时，仅剩的号码可能相邻，网页不承诺号码永远分散。浏览器选号不参与生成开奖结果。

验证：随机选取 5,000 份的唯一性、占用过滤、存储边界、取模拒绝、余量不足、买入入口、钱包与期号变化；线上只读核对 ticketWords 和发布资源。未发送测试购买交易。

## 购买边界验证记录（2026-09-07）

本次不更改或部署合约。使用现有 V5 来源编译的购买逻辑、真实 Solidity 执行与本地 mock BEM/电路依赖，不发送主网购买或领取交易。

| 场景 | 已验证结果 |
| --- | --- |
| 两个钱包各随机买 5,000 份 | 两笔都成功，售出 10,000，未售槽位为 0 |
| 已售全部偶数号码，再随机卖出 2,500 份 | 剩余 2,500 个零散空号仍可全部成交 |
| 仅剩 2,500，申请 5,000 且请求号码均冲突 | 成交 2,500，只扣相应本金，其余金额留在钱包 |
| 最后 1 个号码分别位于 00001、05001、10000 | 都能正常买入，只扣 1 份本金 |
| 钱包已有 4,999 份，再申请 3 份 | 只成交 1 份；达到 5,000 后其他钱包仍可购买 |
| 无授权、余额不足、异常扣税代币 | 交易回退，号码和本金状态均回滚 |
| 空数组、重复、乱序、越界、超过 5,000 或错误期号 | 拒绝请求，不占用剩余号码 |
| 交易 Gas 用尽 | 回退不占号；下个钱包使用足够 Gas 后可购买 |
| 95% 的关闭期限前一秒 / 到达期限 | 前者可买，后者停止销售，未售号码仍保留为空 |
| 募集 24 小时到期未满足封盘 | 停止销售，原购买人的本金符合退款条件 |
| 抢购后收到成交 0 份的回执 | 前端确认 0 扣款、清除 pending，继续允许领取 |
| 随机选号读取失败、钱包变化、期号变化 | 不发送购买，也不回退成连续选号 |

`node --test test/bem-sparkdraw-random-purchase.test.mjs`：9 项通过。
`node --test test/bem-drand-refunds.test.mjs test/bem-sparkdraw-deployment.test.mjs`：7 项通过，包含随机数开奖、奖金领取、退款合并、过期销毁及部署绑定。
常规测试：78 项，77 通过、1 项因 Windows 不支持 POSIX 文件权限而跳过，0 失败；该权限检查已在此前 Linux 服务器验证。

模拟交易实际 Gas：随机首次 5,000 份 15,534,428；剩余 5,000 份 6,032,406；冲突请求填补 2,500 个零散空号 7,539,513。均低于 16,777,216 上限。费用仍按发送时网络价格及页面 0.001 BNB 上限核算，不能保证所有网络价格下都可发送。已经上链的失败或零成交交易仍支付网络 Gas。

测试未发现因号码前后位置或碎片化而永久无法买入的情况。剩余号码不等于始终可买：必须尚在销售期、钱包仍有额度，且余额、授权、Gas 和链上服务正常。随机选号只用于购买，不改变链上开奖算法。
