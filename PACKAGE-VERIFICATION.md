# 独立发布包验证

## 2026-09-07 钱包与退款修复

以实际服务器 `chouj-20260907-admin-account` 的 18 份公开应用源码为基线，保留账号密码后台与 HTTPS 域名配置。修复混合钱包识别、交易加速/取消后的状态追踪、往期退款入口，以及跨标签页覆盖未确认交易的问题。

- `pnpm test`：127 项通过，0 失败、0 跳过，约 100 秒。
- 额外复跑最新页面事件测试：11 项通过；生产玩家页与后台构建成功。
- 0.1 BEM（10 票）和 1 BEM（100 票）在本地合约上验证精确扣款、72 小时前拒退、到期原額退款及重复退款回滚；100 BEM 每期规则不变。
- 使用合成钱包和本地链，未发送主网交易、未读取管理员凭据。页面仍须由用户的钱包签名，售票保持关闭。
- 原已部署的四份 Solidity 文件及原部署证据保持不变。更换实际收款容器需要独立的新合约部署，不能仅修改正式网站地址。
- 当前公开入口为 <https://tapeout.cc.cd/>。浏览器自动化读取页面超时，页面行为通过真实应用代码的内存 DOM 事件测试核验；不把这些结果视为真实钱包的主网交易测试。

以下保留初始导出时的验证记录。

日期：2026-09-06。环境：Windows、Node.js 24.19.0、pnpm 11.19.0。

实际在本发布目录运行：

```sh
pnpm install --offline --frozen-lockfile --ignore-scripts
pnpm run build
pnpm test
```

- 锁文件安装成功，63 个依赖包由本机内容缓存复用。
- 参与者首页及中文后台两个入口构建成功。
- **80 项测试通过，0 失败，0 跳过**，总耗时约 99 秒。
- 覆盖满池开奖、票号归属、500 份单笔 Gas 上限、原子 4/1/95 分配、VRF 请求失败回滚、72 小时退款、60 秒冷却，以及 RPC、管理签名、历史重组、钱包提交守卫与 Keeper 重启恢复。
- Ganache 的原生 µWS 扩展未匹配本机 Node 版本，自动采用其 JavaScript 实现；测试全部通过。
- 测试使用本地链与临时账户，没有发送主网交易。测试生成的三份报告在结束后恢复为源工作区公开证据，保留导出清单中的原始 SHA-256；本文件单独记录发布目录的复验结果。
- 发布文件按清单导出。凭证模式扫描仅命中测试中 `example.invalid` 的虚构 URL，已人工确认；未发现真实私钥、助记词、访问令牌或运行会话文件。

网站已经读取正式合约；真实钱包完整购票、真实 VRF 返回、主网派发与公网服务尚待联调。售票保持关闭。其他现场检查见 [网站验证记录](outputs/bem-raffle-2075/production/WEBSITE-VERIFICATION.md)。
