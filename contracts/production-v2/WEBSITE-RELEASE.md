# 新场次交付与上线状态

日期：2026-09-07。四份主网合约已找回并核验，公开地址及交易证据见项目 README 与 recovered-deployments-verified.json。部署成功与启动开放分别显示。

## 本次可审阅内容

- 四个固定金额的新合约、离线编译包及可由用户钱包签署的部署页 `/deploy-container.html`。
- 1 BEM 内部场，10/50/100 BEM 公开场，均为 10,000 份；金额与 4%/1%/95% 分配随选择更新。
- 单笔 1,000 份、单地址每期累计 5,000 份。已售优选号顺延补齐，保留仍空闲的指定号码，实际分配以合约事件为准。不是保证交易永不失败：份数、余额、Gas 和 VRF 依赖仍会检查。
- 24 小时未完成后开放 24 小时自领本金期。领取期限固定在原筹资截止之后，不因晚执行而延后。领取由用户发起并支付 Gas；逾期销毁仅针对本期未退剩余本金。
- 收款文案只称“容器”。收款地址为 #13061 容器，计算和一次性启动授权仍为原 #2075。
- 奖金区域占满可用空间，增加独立 U / BNB 换算、BEM 单价、报价来源和更新时间。社区说明字号和行距增大，窄屏采用单列奖金卡。
- 真实中奖滚动播报、可暂停动效、支持减少动画偏好；销毁页面显示金额、类型、期号、时间、完整交易哈希。
- 原合约页面 `/legacy.html` 保留历史退款与交易核对。新增首页不会将原地址套用为新场次。

## 1 BEM 测试与正式上线步骤

1. 四档创建交易、运行代码和固定绑定均已核验，已经登记独立地址，无需再次部署。
2. `/start-test.html` 只配置新 1 BEM 合约。先由订阅管理员添加消费者，再由 2075 容器持有人执行启动，附固定协议费 0.0002 BNB 和钱包网络 Gas。每步必须由用户明确确认。
3. 两项权限就绪并且订阅 BNB 余额非零后，测试入口 `/?pool=1` 可使用真实钱包。至少两个钱包各购买最多 5,000 份；单笔 1,000 份 = 0.1 BEM。精确授权和购买是两笔独立确认的交易。
4. 售满时合约请求 VRF，返回后由用户点击测试结算。若一次只推进拒绝采样，页面显示计算推进并允许下一次明确结算。真实 VRF 成功及分账结果尚待用户完成测试；本地测试不能替代主网结果。
5. 退款由用户自行查询期号并领取。自动结算、开下一期和超期销毁仍需后续配置独立执行钱包与持久化服务；现有 V2 planner 只生成计划，不发交易。它必须涵盖当前与历史退款期，零本金期不可重复尝试销毁零金额。
6. 1 BEM 测试完成后，再配置并有序开放 10/50/100 BEM 正式场次。本版本不会启用这些正式场次的交易入口。

`/api/pools` 已返回四份公开部署记录，只有 1 BEM 场会按实时链上配置允许测试。1 BEM 财务模块独立固定地址和代码哈希，不信任可更换的 registry 地址。原 V1 保护保留，正式三档保持禁止写交易。链上状态和待确认交易会重新核对，不把超时、刷新或换钱包当作交易失败。

## 数据来源与确认

- 行情：DEX Screener 的 BSC token-pairs API，只接受准确 BEM 基础币地址，以及指定 USDT/WBNB 报价币地址。默认筛选流动性不少于 100 USD 的交易对，各自选择最高流动性。更新时间是本服务最近一次取得有效报价的时间。失败保留原始更新时间并标记过期；界面清空过期换算。
- 原合约中奖及 4% 销毁：来自原部署的固定 ABI 事件索引。事件和成功回执需位于主链且达到 12 个确认；重组会回滚记录。
- 新场次中奖及超期本金销毁：各档使用独立的固定地址、ABI 和部署区块索引；未达确认数或重组后的孤块记录不会发布。
- 转入黑洞地址不会减少 BEM 合约的 totalSupply。

本次不需要把 SSH 私钥、钱包私钥、后台凭据或环境文件提交到仓库。
