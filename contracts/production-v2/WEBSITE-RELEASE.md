# 新场次交付与上线状态

日期：2026-09-07。下列“已完成”指代码、编译和本地验证，不代表新的主网合约已经存在。

## 本次可审阅内容

- 四个固定金额的新合约、离线编译包及可由用户钱包签署的部署页 `/deploy-container.html`。
- 1 BEM 内部场，10/50/100 BEM 公开场，均为 10,000 份；金额与 4%/1%/95% 分配随选择更新。
- 单笔 1,000 份、单地址每期累计 5,000 份。已售优选号顺延补齐，保留仍空闲的指定号码，实际分配以合约事件为准。不是保证交易永不失败：份数、余额、Gas 和 VRF 依赖仍会检查。
- 24 小时未完成后开放 24 小时自领本金期。领取期限固定在原筹资截止之后，不因晚执行而延后。领取由用户发起并支付 Gas；逾期销毁仅针对本期未退剩余本金。
- 收款文案只称“容器”。收款地址为 #13061 容器，计算和一次性启动授权仍为原 #2075。
- 奖金区域占满可用空间，增加独立 U / BNB 换算、BEM 单价、报价来源和更新时间。社区说明字号和行距增大，窄屏采用单列奖金卡。
- 真实中奖滚动播报、可暂停动效、支持减少动画偏好；销毁页面显示金额、类型、期号、时间、完整交易哈希。
- 原合约页面 `/legacy.html` 保留历史退款与交易核对。新增首页不会将原地址套用为新场次。

## 仍待用户签名后的链上集成

1. 用户在部署页选择档位，连接真实 BNB 主网钱包，核对绑定与估算费用，再分别签署四个部署交易。
2. 核查主链创建回执、至少 12 个确认、创建交易输入、运行代码及固定 getter。将每档独立地址、交易哈希、部署区块和代码哈希登记为公开部署证据。
3. 将新的游戏地址分别添加为对应 VRF 订阅消费者，并确认订阅能承担请求费用。
4. 发布与这四个精确地址绑定的 RPC 白名单、V2 交易前置检查、实际分配事件追踪、V2 历史索引及自动执行器。原 V1 的固定身份检查不能直接放宽后用于 V2。
5. 自动执行器使用独立已配置的钱包与持久化运行服务。现有 V2 planner 只生成计划，不发交易。必须涵盖当前与历史退款期；零本金期调用 openRefunds，不重复尝试 burnUnclaimed(0)。
6. 完成 1 BEM 场真实 VRF 请求、2075 计算、分账、用户自领本金和期限验证后，由原 2075 容器执行一次性启动授权，再有序开放正式场次。

当前 `/api/pools` 的四个 deployment 均为 null，salesEnabled 均为 false。前端有独立禁止写交易保护。未配置交易集成及自动执行服务时，不得只改文案或标记宣称正式开售、自动开奖或自动销毁已经运行。

## 数据来源与确认

- 行情：DEX Screener 的 BSC token-pairs API，只接受准确 BEM 基础币地址，以及指定 USDT/WBNB 报价币地址。默认筛选流动性不少于 100 USD 的交易对，各自选择最高流动性。更新时间是本服务最近一次取得有效报价的时间。失败保留原始更新时间并标记过期；界面清空过期换算。
- 原合约中奖及 4% 销毁：来自原部署的固定 ABI 事件索引。事件和成功回执需位于主链且达到 12 个确认；重组会回滚记录。
- 新场次中奖及超期本金销毁：新地址登记并接入 V2 索引前为空，页面不编造金额、地址或哈希。
- 转入黑洞地址不会减少 BEM 合约的 totalSupply。

本次不需要把 SSH 私钥、钱包私钥、后台凭据或环境文件提交到仓库。
