# V4 开发快照：未完成，不可部署

此目录只在 `codex/formal-v4-work-in-progress` 分支保存当前开发工作，尚未接入生产页面，也没有部署新地址。线上 1 BEM 测试合约未修改。页面协作者应从 `main` 开始工作。

## 目标规则

| 满额场次 | 容器 | 销毁 | 满额奖金 |
| --- | --- | --- | --- |
| 5 BEM | 1% | 3% | 4.8 BEM |
| 10 BEM | 1% | 4% | 9.5 BEM |
| 50 BEM | 1% | 5% | 47 BEM |
| 100 BEM | 1% | 6% | 93 BEM |

每期 10,000 份，单笔及单钱包每期最多 5,000 份。9500 份触发一次 30 分钟封盘计时，截止不超过原 24 小时期限；满额提前封盘。到期按实售份额抽奖、实收金额分配。未售号码必须重新取同一 VRF 随机流中的下一个样本，不能顺延到下一个已售号码。封盘后不允许新增参与。

## 已保存的工作

V4 独立源码、四档编译包、55 个 V1/V2/V3 文件的保持不变校验清单、5000 份 Gas 测试，以及未被页面导入的 `formal-rules.js`。这些是开发产物，不是完成证明。

## 当前阻塞与后续工作

- 最近一次本地 Gas 测试：自动买入 5000 份 **8,080,369 Gas**；分散自选 5000 份 **18,947,541 Gas**，超过测试采用的 **16,777,216** 单笔上限。因此 `bem-formal-v4-gas.test.mjs` 当前应失败，需要继续优化，不能靠抬高前端上限掩盖。
- 已加入的所有 95% 封盘、部分成交、抽样、位图持票代码仍需要完整回归，尤其是边界份数、尾部非法号码、替补环绕、退款负债和实际金额分配。
- 编译规则元数据、前端 V4 ABI/成交事件解析、部署登记、历史索引、自动封盘与结算执行器尚未接通。
- 新正式部署要求使用 `0x7674fa446D42b1f7f150DC5e678cc525d275Ea53`。钱包签署、部署回执核验、VRF 消费者配置和启动尚未执行。

重现当前开发测试（只运行本地 Ganache，不发送主网交易）：

```sh
node scripts/compile_bem_formal_v4.mjs
node --test test/bem-formal-v4-gas.test.mjs
```

编译命令会验证旧版文件保持原字节内容，再重建本目录对应的 V4 编译包。请勿修改旧合约文件或把这个开发分支直接部署到线上。
